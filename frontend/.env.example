# Browser template. Never place service-role, database, or AI-provider keys here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Optional legacy alias: VITE_SUPABASE_ANON_KEY=
# Blank uses Vite's local proxy and Netlify's same-origin API redirect.
VITE_API_URL=
