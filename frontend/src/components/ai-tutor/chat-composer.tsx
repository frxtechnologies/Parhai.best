import { Loader2, Send } from "lucide-react";

export function ChatComposer({value,onChange,onSend,pending,placeholder}:{value:string;onChange:(v:string)=>void;onSend:()=>void;pending:boolean;placeholder:string}){
  return <div className="sticky bottom-0 z-20 bg-gradient-to-t from-slate-50 via-slate-50 to-transparent px-3 pb-4 pt-8 sm:px-6"><div className="mx-auto flex max-w-4xl items-end gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-[0_12px_40px_rgba(15,23,42,.12)]"><textarea value={value} onChange={e=>onChange(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();onSend()}}} rows={2} placeholder={placeholder} className="min-h-14 flex-1 resize-none bg-transparent px-3 py-2 text-sm outline-none" /><button onClick={onSend} disabled={!value.trim()||pending} className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#0B1F3A] text-white transition hover:-translate-y-0.5 hover:bg-[#12345b] disabled:translate-y-0 disabled:opacity-40">{pending?<Loader2 className="h-5 w-5 animate-spin"/>:<Send className="h-5 w-5"/>}</button></div><p className="mt-2 text-center text-[11px] text-slate-400">Enter to send · Shift + Enter for a new line</p></div>
}
