import type { AiSource, Paper, Subject } from "@/api/types";
import { SourceCard } from "./source-card";

export function StudyContextPanel({subject,paper,sources}:{subject:Subject;paper?:Paper;sources:AiSource[]}){
  const screenshots=sources.filter(s=>s.screenshotUrl);
  return <aside className="space-y-4"><section className="rounded-2xl border bg-white p-4 shadow-sm"><h2 className="font-semibold text-[#0B1F3A]">Study context</h2><dl className="mt-3 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-slate-400">Subject</dt><dd className="font-semibold">{subject.name} {subject.code}</dd></div><div><dt className="text-slate-400">Level</dt><dd className="font-semibold">{subject.level.replace("_"," ")}</dd></div><div><dt className="text-slate-400">Paper</dt><dd className="font-semibold">{paper?`${paper.year} · P${paper.paperNumber} · V${paper.variant??"—"}`:"All papers"}</dd></div><div><dt className="text-slate-400">Sources</dt><dd className="font-semibold">{sources.length} verified</dd></div></dl></section><section className="rounded-2xl border bg-white p-4 shadow-sm"><h2 className="font-semibold text-[#0B1F3A]">Latest evidence</h2><div className="mt-3 space-y-2">{sources.slice(0,3).map(s=><SourceCard key={s.chunkId} source={s}/>)}{!sources.length&&<p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-400">Verified sources will appear after your first answer.</p>}</div></section><section className="rounded-2xl border bg-white p-4 shadow-sm"><h2 className="font-semibold text-[#0B1F3A]">Question screenshots</h2><div className="mt-3 space-y-2">{screenshots.slice(0,3).map(s=><a key={s.chunkId} href={s.screenshotUrl!} target="_blank" rel="noreferrer"><img src={s.screenshotUrl!} className="mt-2 max-h-48 w-full rounded-xl border object-contain" alt={`Question ${s.questionNumber??""}`}/></a>)}{!screenshots.length&&<p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-400">Question screenshot not generated yet.</p>}</div></section></aside>
}
